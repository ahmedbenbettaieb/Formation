{% extends 'base.html.twig' %}

{% block body %}
    <h1 style="margin-top: 150px"></h1>
    {{  include('quiz_and_test_front/show_timer_test.html.twig') }}
    <h1  class="text-center"> Test : {{ test.sujet }}</h1>
    <form action="{{ path('validate_test',{'id':test.id}) }}" method="post" id="myForm">
        {% set i = 1 %}
        {% for question in test.questions %}
            {{  include('quiz_and_test_front/_form_questionnaire.html.twig') }}
            {% set i = i + 1 %}
        {% endfor %}
        <input type="text" name="user" value="{{ app.user.id }}" hidden>
        <h4 id="error" style= "color:red"> </h4>
        <button onclick="validerTest();" type="button" style="width: 25%;margin: auto;margin-top: 50px;" class="btn btn-dark btn-lg btn-block">Valider</button>
    </form>
{% endblock %}
