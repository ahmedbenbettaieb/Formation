{% extends 'base.html.twig' %}

{% block body %}
    <h1 style="margin-top: 150px"></h1>
    <h1  class="text-center"> Resultat Test</h1>
    <h1 class="text-center">Sujet : {{ test.sujet }}</h1>
    <h3 class="text-center">Note Obtenue : {{ note }}/{{ totalpoints }}</h3>
    <h3 class="text-center">Pourcentage Bonne Reponse : {{ percent }} %</h3>
    {% if percent < 100 %}
        <div class="row">>
            <div class="col ">
                <h1 class="text-center" style="margin-top: 15px;"><a href="{{ path('take_test',{'id':test.id}) }}">Réessayer</a></h1>
            </div>
        </div>
   {% else %}
       <h4 class="text-center"><a href=""> Félicitations {{ app.user.prenom }}  {{ app.user.nom }}</a> </h4>
       <p class="text-center">Vous avez obtenu votre certificat</p>
            <h1 class="text-center" style="margin-top: 15px;"><a href="{{ path('certificat',{'id':test.id}) }}">Certificat</a></h1>
    {% endif %}
    <div class="row">
        <div class="col">
            <h3 style="margin-top: 75px;" class="text-center"><a href="/quiz_front">Espace Quiz & Test</a></h3>
        </div>
    </div>

{% endblock %}
