{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center" style="margin-top: 150px">Modifier Test</h1>

    {{ include('test/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('test_index',{'id':app.user.id}) }}">back to list</a>

    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
